import type { Metadata } from "next";
import { Roboto } from "next/font/google";
import "./globals.css";

const roboto = Roboto({
  variable: "--font-roboto",
  subsets: ["latin"],
  weight: ["300", "400", "500", "700", "900"],
});

export const metadata: Metadata = {
  title: "1 Click Onboarding Install Pack",
  description:
    "Perfect for coaching and agency businesses looking to fully streamline their client onboarding process. Install an automated & foolproof system used by 7 figure businesses. Try it today for only $47, backed by our 30-day money-back guarantee.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${roboto.variable} antialiased`}>{children}</body>
    </html>
  );
}
